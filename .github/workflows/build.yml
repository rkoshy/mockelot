name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          # Linux AMD64
          - os: ubuntu-latest
            platform: linux/amd64
            name: linux-amd64
            output: mockelot

          # macOS Universal (ARM + Intel)
          - os: macos-latest
            platform: darwin/universal
            name: darwin-universal
            output: Mockelot.app

          # macOS ARM64 only (Apple Silicon)
          - os: macos-latest
            platform: darwin/arm64
            name: darwin-arm64
            output: Mockelot.app

          # macOS AMD64 only (Intel)
          - os: macos-latest
            platform: darwin/amd64
            name: darwin-amd64
            output: Mockelot.app

          # Windows AMD64
          - os: windows-latest
            platform: windows/amd64
            name: windows-amd64
            output: Mockelot.exe

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev

      - name: Build application
        run: wails build -platform ${{ matrix.platform }}

      - name: Create Linux archive
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd build/bin
          tar -czf mockelot-${{ matrix.name }}.tar.gz mockelot
          mv mockelot-${{ matrix.name }}.tar.gz ../../

      - name: Create macOS archive
        if: matrix.os == 'macos-latest'
        run: |
          cd build/bin
          ditto -c -k --sequesterRsrc --keepParent Mockelot.app ../../mockelot-${{ matrix.name }}.zip

      - name: Create Windows archive
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          cd build/bin
          Compress-Archive -Path Mockelot.exe -DestinationPath ../../mockelot-${{ matrix.name }}.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mockelot-${{ matrix.name }}
          path: |
            mockelot-*.tar.gz
            mockelot-*.zip
          retention-days: 5

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          find artifacts -name "*.tar.gz" -exec cp {} release/ \;
          find artifacts -name "*.zip" -exec cp {} release/ \;
          ls -lh release/

      - name: Create checksums
        run: |
          cd release
          sha256sum * > checksums.txt
          cat checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
